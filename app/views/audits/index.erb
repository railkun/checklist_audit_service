<%= react_component("AuditIndex", {
  audits: @audits.map{|a| {
    checklist: a.checklist,
    updated_at: a.updated_at,
    url: audit_path(a),
    edit_url: edit_audit_path(a) }},
  new_audit_url: new_audit_path,
  checklists: @checklists}) %>
